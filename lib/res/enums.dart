enum SignInType { email, msisdn }

enum ProviderStatus { idle, loading, success, error }

enum ProviderModes { general, authSignIn, authSignUp, authEmailVerification, authResendEmail }

enum LeadButtonType { back, close, none }
